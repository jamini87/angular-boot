import { IsoToJSecPipe } from './iso-to-j-sec.pipe';

describe('IsoToJSecPipe', () => {
  it('create an instance', () => {
    const pipe = new IsoToJSecPipe();
    expect(pipe).toBeTruthy();
  });
});
