import { IsoToJMinPipe } from './iso-to-j-min.pipe';

describe('IsoToJMinPipe', () => {
  it('create an instance', () => {
    const pipe = new IsoToJMinPipe();
    expect(pipe).toBeTruthy();
  });
});
