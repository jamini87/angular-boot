import { IsoToJalaliPipe } from './iso-to-jalali.pipe';

describe('IsoToJalaliPipe', () => {
  it('create an instance', () => {
    const pipe = new IsoToJalaliPipe();
    expect(pipe).toBeTruthy();
  });
});
