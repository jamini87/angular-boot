import { IsoToJDayPipe } from './iso-to-jday.pipe';

describe('IsoToJDayPipe', () => {
  it('create an instance', () => {
    const pipe = new IsoToJDayPipe();
    expect(pipe).toBeTruthy();
  });
});
