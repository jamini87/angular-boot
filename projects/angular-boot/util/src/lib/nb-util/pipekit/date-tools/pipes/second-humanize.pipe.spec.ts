import { SecondHumanizePipe } from './second-humanize.pipe';

describe('SecondHumanizePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondHumanizePipe();
    expect(pipe).toBeTruthy();
  });
});
