import { AreYouSureDirective } from './are-you-sure.directive';

describe('AreYouSureDirective', () => {
  it('should create an instance', () => {
    const directive = new AreYouSureDirective();
    expect(directive).toBeTruthy();
  });
});
