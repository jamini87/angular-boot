import { FloatLabelDirective } from './float-label.directive';

describe('FloatLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatLabelDirective();
    expect(directive).toBeTruthy();
  });
});
